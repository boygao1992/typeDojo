{
  "scripts": {
    "start": "node dist/app.js",
    "build": "pulp build -O --to dist/app.js",
    "dev": "yarn build && yarn start"
  },
  "name": "dojo-web-server",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "express": "^4.16.4"
  },
  "devDependencies": {
    "pulp": "^12.3.0",
    "purescript": "^0.12.0",
    "purescript-psa": "^0.7.2"
  }
}
